os: windows
language: sh
python:
 - '3.8'
before_install:
 - choco install python3
 - export PATH="/c/Python38:/c/Python38/Script:$PATH"
 - python -m pip install --upgrade pip wheel
install:
 - pip install -r requirements.txt
script:
 - pytest --cov-config=.coveragerc --cov=sem_size_analysis tests/
 - flake8
after_sucess:
 - codecov